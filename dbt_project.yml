name: 'dbt_scooters'
version: '1.0.0'
profile: 'dbt_scooters' #'profiles.yml' 
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
clean-targets:         
  - "target"
  - "dbt_packages"
models:
  dbt_scooters:
    +materialized: table
    +docs:
      enabled: true
      node_color: "#f1f3f4"  # 


flags:        
  docs:
    node_color: true  #
    +docs:
      css: "custom.css"  #